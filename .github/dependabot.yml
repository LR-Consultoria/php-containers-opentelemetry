version: 2
updates:
  # Keep GitHub Actions up to date (also avoids Node.js runtime deprecations).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "chore(ci)"

  # Track the FrankenPHP upstream base image used by frankenphp/Dockerfile.
  # Dependabot does not scan subdirectories, so the directory must point to
  # the folder that actually contains the Dockerfile.
  - package-ecosystem: "docker"
    directory: "/frankenphp"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "chore(docker)"
